<%= render "navbar" %>
<div class="container">
  <table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Receivers</th>
    </tr>
  </thead>
  <tbody>
    <% @user.receivers.each_with_index do |e, i| %>
      <tr>
        <th scope="row"><%= i + 1 %></th>
        <td><%= link_to e.to_s, receiver_path(e), style: "color: black" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
